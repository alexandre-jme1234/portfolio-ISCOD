version: "3.9"

services:
  db:
    image: mariadb:11
    container_name: portfolio_db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
      MYSQL_USER: ${DB_USER}
      MYSQL_PASSWORD: ${DB_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
      - ./backend/src/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: portfolio_backend
    restart: unless-stopped
    env_file: .env
    environment:
      DB_HOST: ${DB_HOST:-db}
    ports:
      - "5002:5000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./backend/src:/app/src

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: portfolio_frontend
    restart: unless-stopped
    environment:
      # En Docker, Vite proxie /api vers le container backend (DNS interne Docker)
      # En local sans Docker, vite.config.js fallback vers http://localhost:5000
      API_PROXY: http://backend:5000
    ports:
      - "3000:3000"
    depends_on:
      - backend
    volumes:
      - ./frontend/src:/app/src
      # Monte le dossier public en live pour servir les images ajoutées
      # (logos de la frise parcours, etc.) sans rebuild de l'image Docker.
      - ./frontend/public:/app/public

volumes:
  db_data:
