# Copier vers .env (dev) ou .env.production (build) selon le besoin.
# Si VITE_API_URL est vide ou absente, l'app utilise le chemin relatif /api
# (proxy Vite en dev → backend Docker, voir vite.config.js).
VITE_API_URL=https://portfolio-iscod-1.onrender.com
